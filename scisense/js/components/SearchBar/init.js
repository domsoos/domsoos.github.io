// create the search bar to use on /scisense
// the function def can be found at searchbar.js in the components/SearchBar dir

document.addEventListener('DOMContentLoaded', () => {
    createSearchBar('search-bar-container', 'Search for a paper, author, or category');
  });
