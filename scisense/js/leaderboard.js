// scisense/js/leaderboard.js

document.addEventListener('DOMContentLoaded', () => {
  // Access globally defined Firebase Auth and Firestore
  const auth = window.auth;
  const db = window.db;

  // ... rest of your leaderboard.js code ...
});